You are revising a financial-tear-sheet DRAFT to fix specific issues a reviewer raised,
using ONLY the PAYLOAD data. Rewrite the draft so every listed issue is resolved.

Preserve placeholder discipline: every figure stays a {{field_id}} placeholder using only
the field_ids in the data — never write a digit, $, %, or x inline, and do not invent
field_ids. Fix the flagged sentences (remove or correct unsupported claims, causal
assertions, and directionality/materiality errors). Keep everything that was fine. Return
only the revised tear sheet prose, under 250 words.
